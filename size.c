#include<stdio.h>
// program to demonstrate the use of size of operator and showing the size of different data types
int main(){
    int uday;
    printf("%d \n",sizeof(uday));
    printf("%d \n",sizeof(int));
    printf("%d \n",sizeof(long));
    printf("%d \n",sizeof(short));
    return 0;
}